[package]
name="hayro-jpeg2000"
version="0.4.0"
edition="2024"
rust-version="1.90"
license="MIT OR Apache-2.0"
[features]
default=["std"]
std=[]
[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(feature, values("simd", "image", "logging"))'] }
